databaseChangeLog:
  - changeSet:
      id: 1 # идентификатор набора изменений
      author: developer # имя разработчика, который добавил набор изменений
      changes: # список изменений в этом наборе
        - createTable: # команда liquibase — создает таблицу
            tableName: employee # название таблицы
            columns: # колонки к данной таблице
              - column: # колонка
                  name: id # название колонки
                  type: int # тип
                  autoIncrement: true # признак автоинкремента колонки
                  constraints: # ограничения колонки
                    primaryKey: true
              - column:
                  name: number
                  type: int
                  constraints:
                    nullable: false
              - column:
                  name: capacity
                  type: int
                  constraints:
                    nullable: false liquibase/changesets/022023_create_employee.yml
                    databaseChangeLog:
                      - changeSet:
                          id: 1 # идентификатор набора изменений
                          author: developer # имя разработчика, который добавил набор изменений
                          changes: # список изменений в этом наборе
                            - createTable: # команда liquibase — создает таблицу
                                tableName: position # название таблицы
                                columns: # колонки к данной таблице
                                  - column: # колонка
                                      name: id # название колонки
                                      type: int # тип
                                      autoIncrement: true # признак автоинкремента колонки
                                      constraints: # ограничения колонки
                                        primaryKey: true
                                  - column:
                                      name: number
                                      type: int
                                      constraints:
                                        nullable: false
                                  - column:
                                      name: capacity
                                      type: int
                                      constraints:
                                        nullable: false liquibase/changesets/02072023_create_position.yml